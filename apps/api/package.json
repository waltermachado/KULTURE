{
  "name": "@kulture/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "kulture-core: API do site (catálogo, preço, auth, carrinho, pedidos, pagamentos, NF). Fastify + Prisma/Postgres.",
  "main": "src/server.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "vitest run",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:studio": "prisma studio",
    "admin:make": "node --env-file-if-exists=.env scripts/make-admin.js",
    "seed:demo": "node --env-file-if-exists=.env scripts/seed-demo.mjs"
  },
  "dependencies": {
    "@fastify/cookie": "^11.1.2",
    "@fastify/cors": "^11.3.0",
    "@fastify/jwt": "^10.2.2",
    "@fastify/rate-limit": "^11.2.0",
    "@fastify/static": "^10.1.3",
    "@fastify/swagger": "^9.4.2",
    "@fastify/swagger-ui": "^6.1.1",
    "@kulture/shared": "*",
    "@prisma/client": "^6.13.0",
    "argon2": "^0.45.1",
    "fastify": "^5.3.3",
    "nodemailer": "^7.0.13",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "pino-pretty": "^13.0.0",
    "prisma": "^6.13.0",
    "vitest": "^3.2.4"
  }
}
