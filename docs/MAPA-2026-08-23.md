# Mapa das atualizações — sessão de 23/08/2026

> Visão rápida do que mudou, onde está no código e o que falta o dono fazer. Detalhe por área em `CONTEXTO.md` §5.2b, 5.4, 5.5, 5.6b, 5.6c, 5.7, 5.8.

## Situação do git

| Bloco | Estado |
|---|---|
| **A** — US não vaza · By You 35 dias · página do tênis · SMTP + moldura de e-mail · Marketing v1 | **no GitHub** — commit `8b79aef` |
| **B** — links sem Railway · provedor API MailerSend · falhas por destinatário · etapas de rastreio · nota fiscal | **pendente** (working tree: 29 modificados + 10 novos; prompt do Antigravity já entregue) |

Migrações: `marketing` (A) · `marketing_errors`, `order_stages`, `order_invoices` (B) — todas rodam sozinhas no boot (`migrate deploy`); já aplicadas no banco de dev.

---

## Bloco A — já no ar depois do deploy de `8b79aef`

### 1. Tamanho US não aparece mais para o cliente
- Seletor só com numeração BR (sem "US M 7", sem "Aprox.", sem abas Masculino/Feminino); sacola, checkout, `/conta`, e-mails, WhatsApp e `GET /api/orders` (não-admin) com `BR 38` e sem `nikeSize`.
- Backoffice e `order_items.size_label` continuam com o US (`BR 38 (US W 8.5)`) para comprar na Nike.
- Tabelas: `WOMENS_APPROX` até W 19,5, meios em `MENS_APPROX` — os "US M 15/17/18" ganharam BR.
- Código: `packages/shared/src/sizes` (`sizeLabelBr`), `web/lib/format.js#sizeText`, `SizePicker.jsx`, `mailer.js`, `orders/service.js#clientOrderItem`.

### 2. Nike By You — 35 dias para entrega
- Card: "35 DIAS PARA ENTREGA" abaixo do balão BY YOU; seletor cita o prazo. Constante `BY_YOU_DELIVERY_DAYS` em `web/lib/format.js`.

### 3. Página própria do tênis (link do Instagram) — pronta entrega e hypados
- `/pronta-entrega/<slug>` e `/hypados/<slug>` (code `PE-/HY-` também funciona e vira slug). Slug **não muda ao renomear**.
- Sem modal: galeria, preço, tamanhos BR com quantidade, **Comprar agora** / **Adicionar à sacola**, compartilhar; estados esgotado/404.
- Preview do link (WhatsApp/DM): `index.html` com title/canonical/Open Graph do par (`plugins/serve-web.js#productPageHtml`).
- API `GET /api/stock/:ref`. Admin: URL com "Copiar" em `/admin/estoque/:id`; lista com "página ↗".
- Código: `web/pages/StockProduct.jsx`, `stock/service.js#getProductByRef`, `stock/routes.js`.

### 4. E-mail por SMTP + moldura única
- `MAIL_PROVIDER=smtp` (nodemailer; `SMTP_HOST/PORT/SECURE/USER/PASS`, `MAIL_REPLY_TO`), `verify()`/`close()`.
- Todos os e-mails (reset, pedido pago/enviado/entregue/cancelado, venda externa, campanhas) na mesma moldura preta/amarela com links clicáveis (`emailLayout`).

### 5. Marketing v1 — `/admin/marketing`
- Campanha: assunto, parágrafos, imagem e botão opcionais, público (todos · só compradores · só contas) com contagem; prévia; "Enviar teste para mim"; disparo em segundo plano com progresso.
- Descadastro assinado (`GET/POST /api/marketing/unsubscribe`) + `List-Unsubscribe`; caixa "quero receber novidades" em `/conta`; `users.marketing_opt_in` (padrão ligado), `marketing_unsubscribes`, `marketing_campaigns`.
- Código: `modules/marketing/`, `web/admin/Marketing.jsx`.

---

## Bloco B — pendente de commit

### 6. Links públicos nunca com o domínio do Railway ⚠️ (correção do vazamento no "esqueci minha senha")
- `lib/site-url.js`: `canonicalWebUrl` (se `PUBLIC_WEB_URL` for `*.railway.app` em produção → `https://lojakulture.com.br`), `resolveWebUrl` (origem só da allowlist; Railway → canônica; localhost só se for exatamente o site), `requestOrigin`.
- Aplicado em: reset de senha (site + painel), redirect do pagamento, e-mail de pedido pago, venda externa, campanhas, descadastro, OG da página do tênis.
- `/health` → `siteUrl`, `publicWebUrlMisconfigured`; aviso no boot e no card de e-mail do painel. Teste `site-url.test.js`.

### 7. Provedor API da MailerSend completado + diagnóstico de envio
- `MAIL_PROVIDER=mailersend` (token `mlsn.…`): reply-to, anexos, erros legíveis (401 token · 422 remetente/domínio · trial/não aprovada · 429), `verify()` confere o token.
- Painel: com `MAIL_PROVIDER=log` o disparo é **bloqueado** (antes voltava "ok" sem mandar); tabela de campanhas mostra **"X falha(s) — ver quem"** e o provedor usado; teste devolve o id da mensagem. Migração `marketing_errors`.

### 8. Etapas de rastreio do pedido
- Status novos `in_transit` e `arrived_br`. Importado: **Pagamento aprovado → Pedido comprado → Em trânsito internacional → Chegou no Brasil → Enviado pro seu endereço → Entregue**; pronta entrega/hypados pulam as etapas dos EUA (`order.international`).
- E-mail em cada etapa (`buildOrderStageEmail`), reenvio por etapa, fila de entregas com botão da próxima etapa, dashboard/venda externa atualizados.
- Código: `orders/status.js` (fonte única), `web/components/OrderTimeline.jsx` (Rastrear pedido + Meus pedidos), `admin/ui.jsx`, `Deliveries.jsx`. Teste `order-stages.test.js`.

### 9. Nota fiscal — manual agora, Bling depois
- Detalhe do pedido → card **Nota fiscal**: anexar PDF (DANFE) + XML + número/série/chave/emissão; **Enviar ao cliente por e-mail** (anexos); baixar; substituir; remover. Cliente baixa em "Meus pedidos".
- Tabela `order_invoices` (PDF/XML no banco, `source` manual|bling, `sentAt/sentTo`). Mailer com `attachments` (SMTP e API).
- Bling automático: gancho `invoices.issueAutomatically` + `BLING_CLIENT_ID/SECRET` — falta o app OAuth no Bling.
- Código: `modules/invoices/`, `web/admin/OrderDetail.jsx#InvoiceCard`, `web/pages/Account.jsx`. Teste `invoices.test.js`.

---

## Bloco C — tarde de 23/08 (pendente de commit, junto com o B)

10. **ModeBar mobile**: "IMPORTADOS" cortado com 3 abas → colunas 1/3 exatas + bandeira em cima/nome embaixo no celular.
11. **/admin/precos**: acréscimos por tipo de tênis (o LeBron +R$300 saiu do código e virou regra editável; semente no banco).
    R$ e/ou %, escopos nome/SKU/marca/categoria, teste ao vivo, efeito imediato (cache versionado `v7-<versão>`).
12. **Filtro por tamanho** na pronta entrega/hypados (`?tam=41`, chips com os BR disponíveis).
13. **Cupons**: `/admin/cupons` (%, R$, teto, mínimo, validade, limite de usos) + campo na sacola antes de finalizar;
    servidor recalcula o desconto no checkout; uso conta só quando o pedido é pago; desconto em e-mails/painel.
14. **Hypados reposicionados** (noite): abas na ordem Encomenda · **Hypados** · Pronta entrega; textos corrigidos em
    toda parte (barra, hero, marquee, cards, página do tênis, seletor, OG) — hypado NÃO está no Brasil: é difícil de
    achar, **garimpado nos EUA pelos contatos Kulture BR** e importado sob encomenda; pedido de hypado agora segue o
    rastreio internacional completo (`isInternationalOrder` considera `breakdown.section === "hypados"`) e o e-mail de
    "pedido comprado" fala em garimpo; e-mail de pagamento da pronta entrega não fala mais em "comprar nos EUA".
15. **WhatsApp flutuante**: botão verde fixo (canto inferior direito) em toda a loja — o CTA de atendimento deixou de
    ficar "lá embaixo"; desktop com rótulo "Fale com a gente", mobile só a bolinha; sacola/modal cobrem quando abertos.
16. **Bling conectável** (noite): `/admin/bling` — OAuth completo (conectar/status/desconectar, tokens com renovação
    automática, callback `https://lojakulture.com.br/api/bling/callback` que PRECISA estar no app do Bling) + checklist
    fiscal na tela. Emissão da NF-e entra em cima disso quando o checklist estiver ok (nota manual segue valendo).
17. **Tamanhos femininos**: corrigido (opção B do dono) — escala feminina inteira derivada da masculina (W = M + 1,5 →
    tabela masculina): mesmo par físico → mesmo BR (o 41+ não sai mais 0,5 acima; W 5 = 34). Cache do catálogo `v8-*`;
    BR→US do cadastro idem; pedidos antigos não mudam.

## Validação
- API: **86/86** (15 arquivos; novos: `marketing`, `site-url`, `order-stages`, `invoices`, `pricing`, `coupons`), shared **31/31**, `npm run build -w apps/web` ok.
- Nenhum segredo em arquivo (senhas SMTP e token da API ficaram só no chat).

## Pendências do dono (por ordem)
1. **Commit/push do bloco B** (prompt do Antigravity já entregue).
2. Railway → `kulture-api`: `PUBLIC_WEB_URL=https://lojakulture.com.br`, `PUBLIC_API_URL=https://lojakulture.com.br`; e-mail pela API: `MAIL_PROVIDER=mailersend`, `MAILERSEND_API_TOKEN=<token>`, `MAIL_FROM=contato@lojakulture.com.br`, `MAIL_FROM_NAME=Kulture BR` (opcional `MAIL_REPLY_TO`). Deploy.
3. `/admin/marketing` → Testar conexão → E-mail de teste para mim. Se constar "enviado" e não chegar: MailerSend → Activity (rejeição / conta pendente de aprovação). Mandar o que o card e a tabela mostrarem.
4. **Regenerar** as duas senhas SMTP e o token da API que circularam no chat; rotacionar `JWT_SECRET` (pendência antiga).
5. Confirmar o padrão de opt-in de marketing (hoje: ligado para todo mundo, com descadastro fácil).
6. Bling: criar o app OAuth (Configurações → Aplicativos) e passar o client id para implementar a emissão automática.
7. Antigas: 12x vs 5x; `TEST_PRODUCT_ENABLED=false`; `www.lojakulture.com.br`.
